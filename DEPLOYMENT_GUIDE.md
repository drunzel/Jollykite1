# 🚀 Инструкция по деплою JollyKite PWA

## 🔄 ВАЖНО: Автоматическая очистка кэша при каждом деплое

**Теперь при каждом деплое нужно обновлять версию Service Worker!**

### Обязательный шаг перед каждым деплоем:

1. **Откройте файл `sw.js`**
2. **Найдите строку:**
   ```javascript
   const VERSION = '1.2.5'; // Increment this on each deployment
   ```
3. **Увеличьте версию:** `1.2.5` → `1.2.6` → `1.2.7` и т.д.
4. **Сохраните файл**
5. **Закоммитьте и запушьте:**
   ```bash
   git add sw.js
   git commit -m "Bump version to 1.2.6"
   git push origin main
   ```

**Что происходит после этого:**
- Пользователи автоматически получат обновление через 60 секунд (или при перезагрузке)
- Старый кэш будет удалён автоматически
- Страница перезагрузится с новой версией
- В консоли появится: `🔄 New Service Worker activated, reloading page...`

---

## ✅ Готовность к деплою

Все изменения уже закоммичены и отправлены на GitHub:
- ✅ manifest.json обновлен
- ✅ InstallPrompt.js создан
- ✅ Service Worker с автоматическим обновлением (v1.2.5)
- ✅ index.html обновлен с системой автообновления
- ✅ Код запушен в GitHub

---

## 🎯 Варианты деплоя

### **ВАРИАНТ 1: Vercel (Рекомендуется)**

#### Способ A: Через веб-интерфейс (самый простой)

1. **Перейдите на Vercel:**
   - Откройте https://vercel.com/
   - Нажмите "Sign Up" или "Login"
   - Войдите через GitHub

2. **Импортируйте проект:**
   - Нажмите "Add New" → "Project"
   - Выберите репозиторий `drunzel/Jollykite1`
   - Нажмите "Import"

3. **Настройки (обычно не требуются):**
   - Framework Preset: Other
   - Root Directory: `./`
   - Build Command: (оставьте пустым)
   - Output Directory: (оставьте пустым)
   - Install Command: `npm install` (опционально)

4. **Деплой:**
   - Нажмите "Deploy"
   - Дождитесь завершения (1-2 минуты)
   - Готово! Vercel даст вам URL типа `https://jollykite1.vercel.app`

5. **Автодеплой настроен:**
   - При каждом push в GitHub проект будет автоматически обновляться

#### Способ B: Через командную строку

```bash
# 1. Войдите в Vercel
vercel login

# 2. Перейдите в папку проекта
cd /Users/andreyezhov/Jollykite1

# 3. Деплой (первый раз)
vercel

# 4. Производственный деплой
vercel --prod
```

---

### **ВАРИАНТ 2: GitHub Pages**

1. **Откройте настройки репозитория:**
   - Перейдите на https://github.com/drunzel/Jollykite1
   - Нажмите Settings → Pages (в левом меню)

2. **Настройте источник:**
   - Source: Deploy from a branch
   - Branch: `main`
   - Folder: `/ (root)`
   - Нажмите "Save"

3. **Дождитесь деплоя:**
   - GitHub начнет деплой автоматически
   - Обычно занимает 2-3 минуты
   - URL будет: `https://drunzel.github.io/Jollykite1/`

4. **Проверьте статус:**
   - Вкладка Actions → последний workflow
   - Когда появится зеленая галочка - готово!

**⚠️ ВАЖНО для GitHub Pages:**

Нужно обновить `manifest.json` и `sw.js` для работы с поддиректорией:

```bash
# Откройте manifest.json и измените:
"start_url": "/Jollykite1/",
"scope": "/Jollykite1/",

# Откройте sw.js и измените пути:
'/Jollykite1/',
'/Jollykite1/index.html',
'/Jollykite1/manifest.json',
# и т.д.
```

**Или используйте собственный домен** (рекомендуется):
- Settings → Pages → Custom domain
- Введите ваш домен (например, `jollykite.com`)

---

### **ВАРИАНТ 3: Netlify**

1. **Перейдите на Netlify:**
   - https://app.netlify.com/
   - Войдите через GitHub

2. **Добавьте новый сайт:**
   - "Add new site" → "Import an existing project"
   - Выберите GitHub
   - Выберите репозиторий `drunzel/Jollykite1`

3. **Настройки деплоя:**
   - Branch: `main`
   - Build command: (пусто)
   - Publish directory: `.` (корень)

4. **Деплой:**
   - Нажмите "Deploy site"
   - URL: `https://random-name.netlify.app`

5. **Настройте домен:**
   - Site settings → Domain management
   - Можете изменить на свой поддомен

---

### **ВАРИАНТ 4: Cloudflare Pages**

1. **Перейдите в Cloudflare Pages:**
   - https://pages.cloudflare.com/
   - Войдите в аккаунт

2. **Создайте проект:**
   - "Create a project"
   - Подключите GitHub
   - Выберите `drunzel/Jollykite1`

3. **Настройки:**
   - Production branch: `main`
   - Build command: (пусто)
   - Build output directory: (пусто)

4. **Деплой:**
   - "Save and Deploy"
   - URL: `https://jollykite1.pages.dev`

---

## 📱 После деплоя

### 1. Протестируйте на мобильном

**Android (Chrome):**
1. Откройте ваш URL на телефоне
2. Должна появиться кнопка "Установить приложение" внизу
3. Нажмите на неё
4. Приложение установится на рабочий стол

**iOS (Safari):**
1. Откройте ваш URL в Safari
2. Должен появиться баннер с инструкцией
3. Нажмите кнопку "Поделиться" (квадрат со стрелкой вверх)
4. Выберите "На экран Домой"
5. Нажмите "Добавить"

### 2. Проверьте Lighthouse

1. Откройте Chrome DevTools (F12)
2. Вкладка "Lighthouse"
3. Выберите "Progressive Web App"
4. Нажмите "Generate report"
5. Цель: 95+ баллов

### 3. Проверьте Service Worker

1. Chrome DevTools → Application
2. Вкладка "Service Workers"
3. Должен быть зарегистрирован `sw.js` (v1.2.0)
4. Статус: "activated and is running"

### 4. Тест установки

**На компьютере:**
- Chrome/Edge: В адресной строке справа появится иконка "Установить"
- Или: Меню (⋮) → "Установить JollyKite"

**На мобильном:**
- Кнопка установки должна появиться автоматически
- Или через меню браузера

---

## 🎨 Дополнительные настройки

### Собственный домен (опционально)

**Для Vercel:**
```bash
vercel domains add jollykite.com
```

Или в веб-интерфейсе:
- Settings → Domains → Add domain

**Для GitHub Pages:**
- Settings → Pages → Custom domain
- Введите домен
- Настройте DNS (добавьте CNAME запись на `drunzel.github.io`)

**Для Netlify:**
- Site settings → Domain management → Add custom domain

---

## 🔍 Устранение проблем

### Service Worker не регистрируется

```javascript
// Проверьте в консоли браузера:
navigator.serviceWorker.getRegistrations().then(registrations => {
  console.log('Registered SWs:', registrations);
});

// Если нужно сбросить:
navigator.serviceWorker.getRegistrations().then(registrations => {
  registrations.forEach(reg => reg.unregister());
});
location.reload();
```

### Кнопка установки не появляется

1. Убедитесь, что сайт открыт по HTTPS
2. Проверьте manifest.json в DevTools → Application → Manifest
3. Проверьте консоль на ошибки
4. Попробуйте в режиме инкогнито

### PWA не устанавливается на iOS

1. Убедитесь, что используете Safari (не Chrome)
2. Проверьте наличие `apple-touch-icon` в HTML
3. iOS требует HTTPS (localhost работает для тестирования)

---

## ✅ Чеклист готовности

- [x] Код закоммичен и запушен в GitHub
- [x] Service Worker обновлен до v1.2.0
- [x] manifest.json обновлен
- [x] InstallPrompt.js добавлен
- [ ] Проект задеплоен на HTTPS
- [ ] Протестировано на Android
- [ ] Протестировано на iOS
- [ ] Lighthouse score > 95
- [ ] Приложение устанавливается на мобильных

---

## 🎯 Рекомендация

**Для быстрого старта используйте Vercel (Вариант 1, Способ A):**
1. Простая настройка через веб-интерфейс
2. Автоматический деплой при каждом commit
3. Бесплатный SSL сертификат
4. Отличная производительность
5. Готово за 2 минуты

**Команды для Vercel CLI (если хотите через терминал):**
```bash
# Авторизуйтесь в Vercel
vercel login

# Деплой проекта
cd /Users/andreyezhov/Jollykite1
vercel --prod
```

---

## 🆘 Нужна помощь?

- **Vercel Docs:** https://vercel.com/docs
- **GitHub Pages:** https://docs.github.com/en/pages
- **PWA Docs:** https://web.dev/progressive-web-apps/
- **Netlify:** https://docs.netlify.com/

---

**Удачи с деплоем! 🚀**

После деплоя ваше приложение будет доступно как полноценное PWA с возможностью установки на любой мобильный телефон!
