# 🔧 Backend Setup для JollyKite

Пошаговая инструкция по настройке backend с Vercel Functions + Supabase.

---

## 📋 Что мы создали:

1. ✅ **Supabase база данных** - хранит историю измерений ветра
2. ✅ **Vercel Function `/api/collect-wind-data`** - собирает данные каждые 5 минут
3. ✅ **Vercel Function `/api/wind-history`** - API для получения истории
4. ✅ **Vercel Cron** - автоматический запуск сбора данных

---

## 🚀 Шаг 1: Создание проекта Supabase

### 1.1 Регистрация
1. Откройте: https://supabase.com
2. Нажмите **"Start your project"**
3. Войдите через GitHub

### 1.2 Создание проекта
1. Нажмите **"New Project"**
2. Выберите организацию (или создайте новую)
3. Заполните:
   - **Name**: `jollykite`
   - **Database Password**: придумайте надежный пароль (сохраните его!)
   - **Region**: выберите ближайший (например, `Southeast Asia (Singapore)`)
4. Нажмите **"Create new project"**
5. Подождите 2-3 минуты пока проект создается

### 1.3 Получение API ключей
1. В левом меню: **Settings** → **API**
2. Скопируйте:
   - **Project URL** (например: `https://abcdefgh.supabase.co`)
   - **anon public** key (длинная строка)
   - **service_role** key (длинная строка, держите в секрете!)

---

## 🗄️ Шаг 2: Создание таблицы в Supabase

### 2.1 SQL Editor
1. В левом меню: **SQL Editor**
2. Нажмите **"New query"**
3. Скопируйте содержимое файла `supabase/schema.sql`
4. Вставьте в редактор
5. Нажмите **"Run"** (или Cmd/Ctrl + Enter)
6. Вы увидите: `Success. No rows returned`

### 2.2 Проверка
1. В левом меню: **Table Editor**
2. Вы должны увидеть таблицу **`wind_measurements`**
3. Сейчас она пустая - данные появятся после запуска cron

---

## ⚙️ Шаг 3: Настройка Vercel Environment Variables

### 3.1 Vercel Dashboard
1. Откройте: https://vercel.com/dashboard
2. Найдите проект **jollykite** / **jollykite1**
3. Перейдите: **Settings** → **Environment Variables**

### 3.2 Добавление переменных
Добавьте следующие переменные (для **Production, Preview, Development**):

#### SUPABASE_URL
- **Name**: `SUPABASE_URL`
- **Value**: ваш Project URL из Supabase (шаг 1.3)
- **Environment**: выберите все (Production, Preview, Development)

#### SUPABASE_ANON_KEY
- **Name**: `SUPABASE_ANON_KEY`
- **Value**: ваш anon public key из Supabase
- **Environment**: выберите все

#### SUPABASE_SERVICE_KEY
- **Name**: `SUPABASE_SERVICE_KEY`
- **Value**: ваш service_role key из Supabase
- **Environment**: выберите все
- ⚠️ **ВАЖНО**: Это секретный ключ, не делитесь им!

#### CRON_SECRET
- **Name**: `CRON_SECRET`
- **Value**: случайная строка для безопасности
- **Environment**: выберите все

**Как сгенерировать CRON_SECRET:**
```bash
# На macOS/Linux
openssl rand -base64 32

# Или используйте любую случайную строку, например:
# my-super-secret-cron-key-12345
```

---

## 🚀 Шаг 4: Деплой на Vercel

### 4.1 Коммит изменений
Все необходимые файлы уже созданы. Теперь отправим их в GitHub:

```bash
git add .
git commit -m "Add backend with Vercel Functions + Supabase"
git push origin main
```

### 4.2 Автоматический деплой
- Vercel автоматически обнаружит изменения
- Запустится новый деплой (займет 1-2 минуты)
- После завершения backend будет активен!

### 4.3 Проверка деплоя
1. Откройте Vercel Dashboard → ваш проект
2. Перейдите на вкладку **Deployments**
3. Последний деплой должен быть **Ready** ✅

---

## ✅ Шаг 5: Проверка работы

### 5.1 Тестирование API
Откройте в браузере (замените на ваш домен):

**API истории ветра:**
```
https://your-domain.vercel.app/api/wind-history
```

Должны увидеть JSON с данными (или пустой массив, если cron еще не запускался).

### 5.2 Ручной запуск сбора данных
Можно вручную вызвать функцию сбора:

```bash
curl -X GET \
  -H "Authorization: Bearer YOUR_CRON_SECRET" \
  https://your-domain.vercel.app/api/collect-wind-data
```

Замените:
- `YOUR_CRON_SECRET` на ваш CRON_SECRET
- `your-domain.vercel.app` на ваш домен

### 5.3 Проверка в Supabase
1. Откройте Supabase Dashboard
2. **Table Editor** → **wind_measurements**
3. Должны появиться записи с данными о ветре!

---

## 🕐 Как работает Cron

### Расписание
- **Интервал**: каждые 5 минут
- **Cron expression**: `*/5 * * * *`
- **Запускает**: `/api/collect-wind-data`

### Почему 5 минут?
- Vercel Free tier: минимум 1 минута между запусками
- 5 минут - оптимальный баланс между актуальностью и количеством запросов
- За день: 288 запросов (12 в час × 24 часа)

### Изменить интервал
Отредактируйте `vercel.json`:
```json
"crons": [
  {
    "path": "/api/collect-wind-data",
    "schedule": "*/10 * * * *"  // каждые 10 минут
  }
]
```

Cron expressions:
- `*/1 * * * *` - каждую минуту
- `*/5 * * * *` - каждые 5 минут
- `*/15 * * * *` - каждые 15 минут
- `0 * * * *` - каждый час (в начале часа)

---

## 📊 API Endpoints

### GET /api/wind-history
Получить историю измерений ветра.

**Query Parameters:**
- `limit` (default: 100) - количество записей
- `hours` (default: 24) - за сколько часов

**Примеры:**
```bash
# Последние 100 записей за 24 часа
/api/wind-history

# Последние 50 записей за 12 часов
/api/wind-history?limit=50&hours=12

# Все записи за неделю
/api/wind-history?limit=2000&hours=168
```

**Response:**
```json
{
  "success": true,
  "latest": { ... },  // самое свежее измерение
  "history": [ ... ], // массив измерений
  "stats": {          // статистика
    "count": 100,
    "avgWindSpeed": "15.2",
    "maxGust": "22.5"
  }
}
```

### POST /api/collect-wind-data
Запустить сбор данных вручную (защищено CRON_SECRET).

**Headers:**
```
Authorization: Bearer YOUR_CRON_SECRET
```

---

## 🔒 Безопасность

### Row Level Security (RLS)
Supabase таблица защищена RLS:
- ✅ **Публичное чтение** - любой может читать через anon key
- ✅ **Запись только для service role** - только Vercel Function может писать
- ✅ **Нет прямого доступа к service_role key** - он только в Vercel env vars

### Cron защита
- `/api/collect-wind-data` проверяет `CRON_SECRET`
- Без правильного токена - 401 Unauthorized

---

## 🐛 Troubleshooting

### Проблема: Cron не запускается
**Решение:**
1. Vercel Dashboard → Settings → Crons
2. Убедитесь, что cron активен
3. Проверьте логи в Deployments → Functions

### Проблема: Ошибка "Unauthorized" в Supabase
**Решение:**
1. Проверьте SUPABASE_SERVICE_KEY в Vercel
2. Убедитесь, что используете service_role, а не anon key

### Проблема: Данные не сохраняются
**Решение:**
1. Проверьте логи функции в Vercel
2. Проверьте RLS политики в Supabase
3. Убедитесь, что таблица создана (SQL из schema.sql)

### Проблема: API возвращает пустой массив
**Решение:**
- Cron еще не запускался (подождите 5 минут)
- Или запустите вручную: `curl` с CRON_SECRET

---

## 📈 Мониторинг

### Vercel Logs
1. Dashboard → Deployments
2. Кликните на деплой → Functions
3. Выберите функцию → View Logs

### Supabase Logs
1. Dashboard → Logs
2. Фильтр по таблице `wind_measurements`

---

## 💰 Стоимость

### Бесплатные лимиты:

**Supabase Free Tier:**
- ✅ 500 MB база данных
- ✅ 50,000 запросов в месяц
- ✅ 2 GB bandwidth

**Vercel Free Tier:**
- ✅ 100 GB-часов serverless функций
- ✅ Безлимитные cron jobs
- ✅ 100 GB bandwidth

**Для JollyKite:**
- 288 записей в день × 30 дней = ~8,640 записей/месяц
- Размер: ~300 bytes × 8,640 = ~2.5 MB/месяц
- 🎉 **Полностью бесплатно!**

---

## 🎉 Готово!

После выполнения всех шагов у вас будет:

✅ Автоматический сбор данных о ветре каждые 5 минут
✅ История измерений в Supabase
✅ API для получения данных
✅ Все работает 24/7 бесплатно

Frontend автоматически будет использовать новое API! 🚀
